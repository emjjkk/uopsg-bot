name: Keep Bot Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render bot
        run: curl -s https://uopsg-bot.onrender.com/ > /dev/null
